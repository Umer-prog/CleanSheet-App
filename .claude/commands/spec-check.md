# /spec-check command
# Usage: /spec-check
# Use this when you are unsure how something should behave

Read SPEC.md fully.
Then answer the question based strictly on what the spec says.
If the spec does not cover it, say so clearly and ask the user to decide
before writing any code. Do not assume or invent behaviour.
